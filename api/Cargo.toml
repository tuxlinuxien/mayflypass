[package]
name = "api"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = { version = "0.9.0", features = ["postgres", "migrate", "chrono", "json", "runtime-tokio", "tls-rustls", "sqlite"] }
tokio = { version = "1.52.3", features = ["full"] }
